'''
Timeline analysis should correspond to their timed' appearance
'''

from os import listdir
from os.path import isfile, join

# Timeline analysis should correspond to their timed' appearance

# Sift (with exclusion criteria)
class Sifter():

	# I guess this can be global, who cares, really
	librarypath = 'library'

	def __init__(targets):
		assert type(list(keywords)) is List

		if not target_files:
			target_files = grep.
